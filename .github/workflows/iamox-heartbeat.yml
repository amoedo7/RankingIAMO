name: IAMOX Live Heartbeat

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"
  push:
    branches:
      - main
    paths:
      - "iamox/**"
      - ".github/workflows/iamox-heartbeat.yml"
      - "data/competitors.json"
      - "data/attempts.jsonl"
      - "data/earnings.jsonl"
      - "data/latest.json"
      - "data/monetization_playbook.json"

permissions:
  contents: write

concurrency:
  group: iamox-live-system
  cancel-in-progress: false

env:
  IAMOX_REPRODUCTION_ENABLED: "1"
  IAMOX_BIRTHS_PER_PULSE: "1"
  IAMOX_REGISTRY_CAPACITY: "5000"
  IAMOX_BIRTHPLACE: "github:amoedo7/RankingIAMO"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Unit tests
        working-directory: iamox
        run: python -m unittest discover -v -p "test_*.py"
      - name: Run IAMOX life scheduler
        run: python iamox/runner.py
      - name: Verify economic invariant
        shell: python
        run: |
          import json
          from pathlib import Path
          q = json.loads(Path("iamox/tasks/queue.json").read_text())
          bad = [x for x in q if str(x.get("verified_net_profit_eur", "0.00")) != "0.00"]
          if bad:
              raise SystemExit("IAMOX runtime may not create verified profit; only the payment verifier can do that")
          print("Economic truth invariant OK")
      - name: Persist heartbeat, births and world state
        run: |
          git config user.name "IAMOX Runtime"
          git config user.email "actions@users.noreply.github.com"
          git add data/competitors.json iamox/state iamox/tasks iamox/cells iamox/handoffs iamox/reports iamox/world
          if git diff --cached --quiet; then
            echo "No state change"
            exit 0
          fi
          git commit -m "chore(iamox): living heartbeat and authorized reproduction"
          git pull --rebase origin main
          git push origin HEAD:main
